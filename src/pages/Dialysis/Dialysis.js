import React, { Component, Fragment } from "react";
import DialysisMenus from "../../components/Dialysedaten/DialyseDaten";
const Dialysis = (props) => {
  // card, dialysedaten, medikationsplan, beratung
  return <DialysisMenus />;
};

export default Dialysis;
